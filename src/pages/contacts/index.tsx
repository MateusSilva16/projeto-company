import { usePageTitle } from "@/hooks/usePageTitle";
import { Container } from "./styles";

const Contacts = () => {
  usePageTitle('Project Commpany - Contatos');
    return <Container>Contacts</Container>;
  };
  
  export default Contacts;